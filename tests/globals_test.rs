// use proto_pdk_test_utils::*;
// use starbase_sandbox::create_empty_sandbox;

// This won't work because we change the home directory for the sandbox,
// but the `bun` command writes to the original home directory.
// generate_globals_test!("bun-test", "cowsay");
